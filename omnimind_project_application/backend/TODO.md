# Backend run checklist

- [ ] Verify working directory and Python environment
- [ ] Ensure dependencies are installed (`pip install -r requirements.txt`)
- [ ] Ensure `.env` contains `GROQ_API_KEY`
- [ ] Run FastAPI backend using uvicorn
- [ ] Hit `/health` endpoint to confirm server is up

