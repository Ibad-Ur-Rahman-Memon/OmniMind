# TODO_BUILD_APK

## Goal
Build a **debug APK** for the Flutter Android app.

## Steps
1. Run `flutter pub get` in `omnimind_project/flutter_app/omnimind/`.
2. Run `flutter build apk --debug`.
3. Confirm APK output location:
   - `omnimind_project/flutter_app/omnimind/build/app/outputs/flutter-apk/`
4. (Optional) If install is needed, run `flutter install`.

## Progress
- ✅ Completed — Debug APK built successfully on 2026-06-03
- APK size: ~156 MB (debug build)
- Location: `build/app/outputs/flutter-apk/app-debug.apk`

