# TODO - Remove patient dashboard background image

- [ ] Update `lib/features/dashboard/patient_home.dart` to remove the `Image.asset('assets/images/back_login.png')` layer from the dashboard background stack.
- [ ] Ensure the `Scaffold`/`Stack` still renders with the dark overlay only.
- [ ] Run Flutter analyze/build (optional) to ensure no compilation issues.

